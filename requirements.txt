# ── Core API stack ────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0

# ── LLM client ────────────────────────────────────────────────────────────────
openai>=1.58.1,<2.0.0

# ── openenv-core runtime deps ─────────────────────────────────────────────────
# Installed with --no-deps in Dockerfile due to broken openai>=2.7.2 metadata.
# All actual runtime imports satisfied explicitly here.
typer>=0.9.0
rich>=13.0.0
pyyaml>=6.0
huggingface_hub>=0.20.0
fastmcp>=0.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
requests==2.32.3